import styled from "styled-components";

const Header = styled.header`
  background-color: #29648A;
  box-shadow: 0 10px 10px rgb(47, 74, 103, 0.4);
  //display: flex;
  padding: 20px 0;
  position: sticky;
  top: 0;
  z-index: 999;
`

export const S = {
    Header,
}